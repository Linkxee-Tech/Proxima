export default function ApprovalCard({ title, requester, priority='normal', onApprove, onReject, onDefer }: { title:string; requester?:string; priority?:string; onApprove:()=>void; onReject:()=>void; onDefer:()=>void }) { return <article className="resource-card"><strong>{title}</strong><span className="muted">{requester || 'Proxima'} · {priority}</span><div className="action-row"><button className="primary" onClick={onApprove}>Approve</button><button className="secondary" onClick={onReject}>Reject</button><button className="ghost" onClick={onDefer}>Defer</button></div></article>; }
