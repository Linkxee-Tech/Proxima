export default function MemoryCard({ text, source, timestamp, onEdit, onDelete }: { text:string; source?:string; timestamp?:string; onEdit:()=>void; onDelete:()=>void }) { return <article className="resource-card"><strong>{text}</strong><span className="muted">{source || 'Manual memory'} · {timestamp || 'now'}</span><div className="action-row"><button className="ghost" onClick={onEdit}>Edit</button><button className="secondary" onClick={onDelete}>Delete</button></div></article>; }
