export default function ConnectionStatus({ name, connected, scopes=[], onConnect, onDisconnect }: { name:string; connected:boolean; scopes?:string[]; onConnect:()=>void; onDisconnect:()=>void }) { return <article className="connection-card"><div><strong>{name}</strong><p className={connected?'connected':'muted'}>{connected?'Connected':'Disconnected'}</p><small>{scopes.join(', ')}</small></div><button className={connected?'secondary':'primary'} onClick={connected?onDisconnect:onConnect}>{connected?'Disconnect':'Connect OAuth'}</button></article>; }
