type Metric = { platform:string; likes?:number; shares?:number; comments?:number; clicks?:number };
export default function AnalyticsChart({ data=[] }: { data?:Metric[] }) { return <section className="usage-grid" aria-label="Social analytics">{data.length ? data.map((metric)=><article className="detail-metric" key={metric.platform}><span className="metric-label">{metric.platform}</span><strong className="metric-value">{(metric.likes||0)+(metric.shares||0)+(metric.comments||0)+(metric.clicks||0)}</strong><small>engagements</small></article>) : <p className="details-empty compact">Analytics will appear after a provider publishes a post.</p>}</section>; }
